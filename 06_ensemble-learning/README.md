# 集成学习
这章节是集成学习，包含了四种算法，分别是随机森林，Adaboost，GBDT，XGboost，由于这章节的案例比较多，所以都写在这里作为总结，更准确的来讲是一个API调用的总结。

## 随机森林
随机森林的思想就是通过并行处理，把很多个弱模型组成一个强模型，在API的运用中，最主要的区别就是设置时需把树设置得比其他三种模型深一点。

## Adaboost + GBDT + XGboost
这三款算法都是串行处理，最核心的就是下一个模型解决前一个模型未做完的事情，但三者关系属于是升级版，最终的XGboost是工业级的。
